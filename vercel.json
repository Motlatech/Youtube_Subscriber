{
    "version": 2,
    "functions": {
        "api/*": {
            "runtime": "@vercel/node",  
            "timeout": 60
        }
    },
    "routes": [
        {
            "src": "/.*",
            "dest": "/server.js"
        }
    ]
}
